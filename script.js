function rechache() {
    let input = document.getElementById("search").value;

    if (input === "") {
        alert("Tanpri antre non yon ekip oswa yon jwè.");
    } else {
        alert("Rezilta rechèch pou: " + input);
    }
}
